# S094 META — UI Round 2: 4 MVP-fit mockups → 4 sub-specs

> **Status**: ✅ shipped (META complete, 4/4 sub-specs shipped 2026-05-02, ~29 pts vs estimate 38-41)
> **Type**: META spec (planning sequence; sub-specs do actual ship)
> **Estimate**: M-L total (~38-41 pts across 4 sub-specs); each sub-spec independent ship
> **Source of truth**: `docs/grimo/ui/prototype/*.html` mockups + `docs/grimo/ui/README.md` 設計說明 + `DESIGN.md` 設計系統

## §1 Goal

Skills Hub 既有 frontend 已 ship 5 pages (HomePage / PublishPage / SkillDetailPage / AnalyticsPage / NotFoundPage) 透過 S085-S088 + S089 對齊到 prototype。**12 個 prototype mockups 中 7 個未實作**，本 META 推進其中 4 個 MVP scope-fit：

```
docs/grimo/ui/prototype/
├── ✅ skills_hub_homepage_mockup.html              → HomePage (S085 v2.63.0)
├── ✅ skill_publish_upload_flow.html               → PublishPage (S086 v2.64.0)
├── ✅ skill_publish_failure_and_high_risk_states.html → PublishPage (S086 v2.64.0)
├── ✅ skill_detail_page_docker_compose_helper.html → SkillDetailPage (S087 v2.65.0)
├── ✅ platform_analytics_dashboard_admin_view.html → AnalyticsPage (S088 v2.66.0)
│
├── 🚧 my_skills_author_dashboard.html              → S094a My Skills (P6 SBE missing)
├── 🚧 semantic_search_results_page.html            → S094b Semantic Search Results
├── 🚧 empty_state_collection_four_tones.html       → S094c Empty State Collection
├── 🚧 docs_page_write_your_first_skill.html        → S094d Docs Walkthrough
│
├── ⏸  admin_review_queue_and_detail.html           → defer (PRD B6 Backlog)
├── ⏸  onboarding_wizard_step_2_of_4.html           → defer (post-MVP)
└── ⏸  skills_hub_landing_page.html                 → defer (marketing landing post-MVP)
```

Per CLAUDE.md「Feature First, Security Later」+ MVP scope，admin/onboarding/landing 三個 mockup 為 post-MVP（B6 已在 PRD Backlog；onboarding/landing 是 marketing-tier）。

## §2 Approach

### Sub-spec sequencing

| # | Spec | Page / Topic | Estimate | Backend impact | PRD link |
|---|------|--------------|----------|----------------|----------|
| 1 | **S094c** | Empty State Collection | XS (5-6) | 0 | UX polish across 4 page |
| 2 | **S094d** | Docs Walkthrough | XS (5-6) | 0 | P2 entry point |
| 3 | **S094a** | My Skills (Author Dashboard) | M (12-13) | `?author=` filter + 30d trend | P6 SBE 「作者查看自己的數據」 |
| 4 | **S094b** | Semantic Search Results | M (12-13) | LLM intent summary endpoint | P5 polish + concept chips |

**Order rationale**: `size 小先`（C/D 兩個 XS pure-FE 快速 ship 增加可重用 EmptyState / Markdown components）→ `共享 component 抽取先`（C 提供 EmptyState 給 A 用）→ `foundation 先`（A 落 author scope，B 才能 reuse SearchResultCard 模板）。

### Reuse posture（per Maximize framework reuse）

既有 components 可直接 reuse:
- `AppShell` — page chrome（4 個 sub-specs 全用）
- `IconTile` — category tint（A: table column / D: bundle tree icon）
- `MetricCard` — 4-metric strip（A: hero metrics）
- `RiskBadge` — risk pill（A: table row / B: result card）
- `SkillCard` — search result template（B: extend with similarity score + why-match）
- `BeamFrame` — primary CTA wrapper（C: empty-state primary action / D: upload bundle CTA）
- `SearchBar` — header（B: with semantic dot variant）

新增 components（最小集，每個 sub-spec 提一個）:
- `EmptyState` (C) — 4-tone variant, reusable across pages
- `MarkdownDoc` (D) — markdown render wrapper, lightweight (用 `react-markdown@latest`)
- `Sparkline` (A) — inline mini-chart for table rows
- `ConceptChip` / `RefineChip` (B) — chip with × / ↗ affordance

不重新發明 wheel：
- Table component → 用 native `<table>` + Tailwind class（既有 `MetricCard` 是 hairline border 模式；同 pattern 套 table）
- Tabs → 用 既有 `SkillDetailPage` 的 tab pattern（同等不抽組件，避免 over-engineer）
- Stepper（D 的 prev/next） → inline `<a>` 即可

### Confidence classification (Research Sufficiency Gate)

| Decision | Confidence | Source |
|----------|------------|--------|
| 既有 5 components reusable as-is | **Validated** | source 已讀 (S085-S088 ship) |
| `react-markdown` 適合 D 的 docs render | **Validated** | npm-registered, MIT, React 19 compatible |
| `?author=` filter 加進 `GET /skills` 為小改動 | **Validated** | `SkillQueryService.search` 已支援 keyword/category 同 pattern |
| 30d per-skill trend 需新 endpoint | **Validated** | `download_events` 表已有 (skill_id, occurred_at)；新增 `/skills/{id}/analytics/trend?days=30` |
| LLM intent summary 設計 | **Hypothesis** | Spring AI 既有，但「intent summary + concept extraction」是新 chat call；prompt 設計 + cost control 待 POC |

**S094b POC required**: intent summary 的 prompt 設計與 latency 預算（target ≤2s P95）；否則 fallback 為 client-side "echo your query" 而非真 LLM intent extraction.

## §3 Acceptance Criteria — META level

每個 sub-spec 自己有完整 AC；META 層 AC 為 sub-spec sequencing + cross-cutting 一致性：

| AC | Case | Expected |
|----|------|----------|
| AC-META-1 | 4 sub-specs 都 ship 後 mockup 對應 frontend page 完整 | spec-roadmap 顯 4 個 ✅ + archive 4 個 spec doc |
| AC-META-2 | 共享 EmptyState component (S094c) reusable in S094a & S094b | A/B sub-specs 自己 import 不重新實作 |
| AC-META-3 | 既有 components 不被破壞 | frontend tests 18 → 18+ PASS（每 sub-spec ship 後 incremental) |
| AC-META-4 | Mockup-to-page 1:1 對應 README 描述 | 每 sub-spec §7 含 prototype HTML reference + 對齊度 checklist |

## §4 Sub-spec snapshots

### S094a — My Skills (Author Dashboard)

**Mockup**: `my_skills_author_dashboard.html`（README ll.69-87）
**Route**: `/my-skills`
**Estimate**: M (12-13)

**Layout**:
- Hero: `7 skills you've published as platform-team` + Publish CTA (BeamFrame)
- 4 summary MetricCards: Total skills (status breakdown) / Total downloads (week delta) / Avg rating / Open flags (red delta if >0)
- Tabs: All / Published / Pending review / Drafts
- Table columns: Skill (name+author+meta) / Status (pill) / Downloads 30d (number + Sparkline) / Latest version + date / Rating / ⋯
- Special row treatments: Pending row amber tint / Draft row 0.85 opacity gray

**Backend touch**:
- Add `?author=` to `GET /skills` (query param only; existing pageable preserved)
- New `GET /api/v1/skills/{id}/analytics/trend?days=30` (per-skill download bucket array for sparkline)

**FE new components**: `Sparkline.tsx`

### S094b — Semantic Search Results

**Mockup**: `semantic_search_results_page.html`（README ll.111-133）
**Route**: `/search?q=...&mode=semantic`
**Estimate**: M (12-13)

**Layout**:
- SearchBar with semantic mode dot pulse (purple #7F77DD)
- AI intent summary card (purple #EEEDFE bg) + concept chips (× to remove)
- Results meta: `ranked by semantic similarity · embeddings via Gemini · 7 skills`
- Top match: gradient bg + star badge + 0.94 score
- Each result card: description with concept highlights + why-match reasoning (gray sub-card) + similarity score with mini bar
- Refine chips: `Only low-risk skills` / `For Kubernetes specifically` / etc.

**Backend touch**:
- New `POST /api/v1/search/intent` (input: query string; output: `{summary, concepts: string[]}`); LLM call (Gemini); 5-min response cache
- Reuse `GET /api/v1/search/semantic` for ranking

**POC required**: intent summary prompt + latency budget; fallback to client-side echo if POC HALT.
**FE new components**: `ConceptChip.tsx` / `RefineChip.tsx` / `IntentSummaryCard.tsx`

### S094c — Empty State Collection

**Mockup**: `empty_state_collection_four_tones.html`（README ll.221-243）
**Estimate**: XS (5-6)

**4 Variants**:
1. **Fresh deployment (Seeding)** — for HomePage when 0 skills exist; purple eyebrow + dashed ghost cards + `Publish the first skill` CTA
2. **New author (Invitational)** — for `/my-skills` when author has 0 skills; horizontal 4-step flow + `Get the starter template` link
3. **No search results (Redirecting)** — for `/search?q=...` when 0 results; echo query + 4 suggestion cards
4. **Review queue all clear (Celebratory)** — admin context (defer to S094e B6); for now 留 hook 給 future spec

**FE new components**: `EmptyState.tsx` with `tone: 'seed' | 'invite' | 'redirect' | 'clear'` prop

**Integrations**:
- HomePage when `total === 0`
- Future: MySkills when `mySkills.length === 0` (reuse via S094a)
- Future: SearchResults when `results.length === 0` (reuse via S094b)

### S094d — Docs Walkthrough

**Mockup**: `docs_page_write_your_first_skill.html`（README ll.245-279）
**Route**: `/docs/your-first-skill`
**Estimate**: XS (5-6)

**Layout**:
- Top app shell with Docs nav active
- Left sidebar IA: Getting started / Reference / Publishing / API & webhooks
- Main column: Meta row (Updated · 5 min read · agentskills.io v1.2 · Edit on GitHub) → Intro → Minimum viable skill code → Bundle tree with badges → Required fields cards → Description A/B compare → Risk tiers pills → CTA block → prev/next nav

**Content source**: Single markdown file `frontend/src/docs/your-first-skill.md` rendered via `react-markdown`. AppShell + Docs sidebar are React components; markdown is content-only.

**FE new components**: `DocsLayout.tsx` / `DocsSidebar.tsx` / `MarkdownDoc.tsx` (wrap react-markdown)
**Dependencies**: `react-markdown@latest` (small ~30KB gz)

## §5 File plan

```
frontend/src/
├── pages/
│   ├── MySkillsPage.tsx                     ← S094a
│   ├── SearchResultsPage.tsx                ← S094b
│   ├── docs/
│   │   ├── DocsLayout.tsx                   ← S094d
│   │   └── YourFirstSkillPage.tsx           ← S094d
│   └── (existing pages unchanged)
├── components/
│   ├── EmptyState.tsx                       ← S094c
│   ├── EmptyState.test.tsx                  ← S094c
│   ├── Sparkline.tsx                        ← S094a
│   ├── ConceptChip.tsx                      ← S094b
│   ├── RefineChip.tsx                       ← S094b
│   ├── IntentSummaryCard.tsx                ← S094b
│   ├── DocsSidebar.tsx                      ← S094d
│   └── MarkdownDoc.tsx                      ← S094d
├── docs/
│   └── your-first-skill.md                  ← S094d (content)
└── App.tsx                                  ← updated routes (each sub-spec)

backend/src/main/java/io/github/samzhu/skillshub/
├── skill/query/SkillQueryService.java       ← S094a (?author= filter)
├── skill/query/SkillQueryController.java    ← S094a (?author= param)
├── analytics/AnalyticsController.java       ← S094a (/skills/{id}/analytics/trend)
├── analytics/AnalyticsQueryService.java     ← S094a (per-skill trend)
└── search/SearchIntentController.java       ← S094b (POST /search/intent, NEW)
└── search/SearchIntentService.java          ← S094b (LLM call)
```

## §6 Acceptance verification

Run: `cd frontend && npm test` (per spec sub-AC) + `cd backend && ./gradlew test`
Pass: 各 sub-spec 自己的 AC 對應 vitest test 帶 `S094[a-d]` tag 全 PASS。

Per `qa-strategy.md`，pipeline 不變；不引入 per-spec shell script。

## §7 Result

**META complete 2026-05-02**：4/4 sub-specs shipped per planned C→D→A→B order；total ~29 pts vs estimate 38-41（trim deferred to polish backlog rather than cut）。

### Sub-spec rollup

| # | Spec | Ship | Pts | Highlight |
|---|------|------|-----|-----------|
| 1 | S094c — Empty State Collection | `v2.69.0` | 5 | `EmptyState.tsx` 4-tone (seed/invite/redirect/clear)；HomePage keyword/semantic 0-results 全改用之；FE tests 18→23 |
| 2 | S094d — Docs Walkthrough `/docs/your-first-skill` | `v2.70.0` | 5 | `YourFirstSkillPage.tsx` single-page JSX no parser dep + DocsLayout/Sidebar；AppShell 加「文件」nav；FE tests 23→28（react-markdown trim 為 inline JSX 省 30KB） |
| 3 | S094a — My Skills (Author Dashboard) `/my-skills` | `v2.71.0` | 9 | `?author=` filter bypass PUBLISHED for author view；MySkillsPage hero + 4 metrics + tabs + table rows；EmptyState invite reuse；Sparkline 暫缺 polish |
| 4 | S094b — Semantic Search Results `/search` | `v2.72.0` | 9-10 | `/search?q=` 專屬 route + LLM intent summary card with graceful fallback (Optional<ChatClient>)；IntentSummaryCard purple bg + concept chips display |

Cumulative pts after S094b ship = 619（roadmap）。

### AC validation

| AC | Status | Evidence |
|----|--------|----------|
| AC-META-1 — 4 sub-specs ✅ + archived | ✅ | roadmap rows 194-197 全 ✅；`docs/grimo/specs/archive/2026-05-02-S094[a-d]-*.md` 4 docs |
| AC-META-2 — `EmptyState` reusable | ✅ | S094a 直接 import 使用 invite tone；S094b 使用 redirect tone（HomePage 0-results 也已切換） |
| AC-META-3 — 既有 components 不被破壞 | ✅ | FE tests 18 → 28 PASS（每 sub-spec ship 後 incremental 增加） |
| AC-META-4 — Mockup-to-page 1:1 對齊 | ✅ | 各 sub-spec §7 含 prototype HTML reference + 對齊度 checklist |

### Lessons learned

- **「Size 小先 + 共享 component 抽取先」排序奏效**：先 ship XS 的 S094c (EmptyState) 讓後續 S094a/b 直接 reuse，省掉重新實作 + 確保視覺一致。同類 META 排序 heuristic 可重用。
- **Sub-spec POC 風險先扛**：S094b 提早旗標「LLM intent summary 是 Hypothesis」+ 寫好 graceful-fallback path（`Optional<ChatClient>`），實作時 Spring AI 真的 unavailable 時 component 仍 render 不卡 ship。POC-HALT 預防勝過事後補救。
- **Trim is moving work, not cutting work**：S094a 的 Sparkline / S094b 的 refine chips 列為 polish defer 而非「bug」，留在 polish backlog 而非從 spec 刪。Roadmap audit trail 看得到這些 deferred 項。
- **META row roadmap-side 可先標 ✅，doc-side closeout 做為獨立 commit**：sub-spec 全 ship 時 roadmap row 自然會在最後一個 sub-spec ship 時更新 ✅，但 META spec doc §1/§7 + archive mv 是 doc-only commit，不該 bundle 進 sub-spec ship commit（per NEVER bundle drive-by refactors）。

### Defer carryover

- **S094e admin review queue** — PRD B6 backlog；待 B6 promoted to MVP 才啟動
- **S094f onboarding wizard** — 等 PRD 增 onboarding requirement
- **S094g landing page** — 等 PRD 增 marketing landing requirement
- **Polish backlog**: S094a Sparkline 真實圖（目前 placeholder）、S094b refine chips 互動 wiring
