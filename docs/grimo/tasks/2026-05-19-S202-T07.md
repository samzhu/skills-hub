# S202-T07: V07 gate, documentation sync, and final verification

## 對應規格
S202：Production E2E Fixture Runner

## 這個 task 要做什麼
這個 task 完成後，`./scripts/verify-all.sh` 的 V07 仍是 `cd e2e && npx playwright test --grep @happy-path`，但它跑的是 production Compose target。PRD、architecture、development standards、QA strategy、test-case ledger、glossary 都會把 E2E fixture 標準改成 `e2e/fixtures` + manifest，不再教後續 spec 新增 `/internal/test/*` endpoint。

## 使用者情境（BDD）
Given（前提）T01-T06 已完成
When（動作）執行 `./scripts/verify-all.sh`
Then（結果）V01-V08 critical gates PASS，且 V07 開啟 `http://localhost:8080` production static app
And（而且）執行 `rg 'Pattern 1|TestDataController|/internal/test|application-e2e' docs/grimo/PRD.md docs/grimo/architecture.md docs/grimo/development-standards.md docs/grimo/qa-strategy.md docs/grimo/test-cases.md docs/grimo/glossary.md docs/grimo/adr`
Then（結果）只允許出現在歷史說明、archive spec、或明確標為 superseded 的 ADR-007 文字
And（而且）`docs/grimo/specs/spec-roadmap.md` 的 S202 狀態反映目前 task loop 狀態

## 研究來源
- `docs/grimo/specs/2026-05-19-S202-production-e2e-fixture-runner.md` §4.8 / §5
- `docs/grimo/adr/ADR-007-browser-e2e-playwright.md`
- `docs/grimo/adr/ADR-008-production-e2e-fixture-runner.md`
- `docs/grimo/PRD.md`
- `docs/grimo/architecture.md:314-317`
- `docs/grimo/development-standards.md` Browser E2E section
- `docs/grimo/qa-strategy.md:70`
- `docs/grimo/test-cases.md`
- `docs/grimo/glossary.md:46`
- `scripts/verify-all.sh:105-117`

## 先做 POC
- POC：not required — 此 task 是文件與 verification registry 同步；前面 T01-T06 已完成 runtime POC。
- Fixture：
  - `positive-fixture`: docs 指向 ADR-008 / `e2e/fixtures`
  - `negative-fixture`: docs 仍要求新增 `TestDataController` → doc scan fail

## 正式程式怎麼做
- 更新 `scripts/verify-all.sh`（如需要）
  - V07 命令保留 `cd e2e && npx playwright test --grep @happy-path`
  - 不加 Playwright `--no-deps`
  - 若 artifact scan 未接進 Gradle `check`，在 verify path 補明確 gate
- 更新 docs：
  - `docs/grimo/PRD.md`
  - `docs/grimo/architecture.md`
  - `docs/grimo/development-standards.md`
  - `docs/grimo/qa-strategy.md`
  - `docs/grimo/test-cases.md`
  - `docs/grimo/glossary.md`
  - `docs/grimo/specs/spec-roadmap.md`
- 確認 ADR：
  - ADR-007 保留 Playwright/e2e workspace 選型，但 Pattern 1 標 superseded
  - ADR-008 記錄 production image + external fixture runner
- 跑 final commands 並把結果寫回 S202 §7（Phase 4 時）

## 單元測試 / 整合測試
- 文件 scan：
  - `@S202 @AC-S202-8: docs no longer instruct new TestDataController fixture pattern`
  - `@S202 @AC-S202-8: PRD and test-case ledger describe production-image E2E gate`
- Full verification：
  - `./scripts/verify-all.sh`

## 會改哪些檔案
- `scripts/verify-all.sh`
- `docs/grimo/PRD.md`
- `docs/grimo/architecture.md`
- `docs/grimo/development-standards.md`
- `docs/grimo/qa-strategy.md`
- `docs/grimo/test-cases.md`
- `docs/grimo/glossary.md`
- `docs/grimo/specs/spec-roadmap.md`
- `docs/grimo/specs/2026-05-19-S202-production-e2e-fixture-runner.md`

## 驗證方式
執行：`./scripts/verify-all.sh`

## 前置條件
- S202-T01 PASS
- S202-T02 PASS
- S202-T03 PASS
- S202-T04 PASS
- S202-T05 PASS
- S202-T06 PASS

## 狀態
PASS（2026-05-19）

## 本輪結果（2026-05-19）

- `e2e/playwright.config.ts` 會在 `npx playwright test --grep @happy-path` 自動打開 semantic fixture gate；缺少 `SKILLSHUB_E2E_GENAI_API_KEY` 時 setup 直接 fail，訊息是 `semantic E2E requires SKILLSHUB_E2E_GENAI_API_KEY`。
- 從 `backend/config/application-secrets.properties` 讀取 `skillshub.genai.api-key` 並匯出成 `SKILLSHUB_E2E_GENAI_API_KEY` 後，`cd e2e && npx playwright test --grep @happy-path` PASS：16 passed。
- V07 第一次用真 Gemini embedding 跑過 setup 後，暴露 3 個舊 browser spec 假設：`/browse` 搜尋已由 S189 改成只打 semantic API，且 S202 setup fixtures 會先放入 production fixture skills，不再用 in-app reset 取得 per-test 空 DB。已更新 `S140-critical-path-browse-search.spec.ts`、`S140-critical-path-semantic-search.spec.ts`、`S172-responsive-polish.spec.ts`，讓它們驗現在的正式 `/browse` 行為；空結果按鈕/連結仍由前端 component tests 覆蓋。
- `cd e2e && npx playwright test --project chromium --grep "AC-4: 從詳情頁下載"` PASS，證明不需要 semantic key 的 production image + fixture manifest + mock OAuth + download path 正常。
- `cd backend && ./gradlew test --tests io.github.samzhu.skillshub.shared.ai.AiModelConfigTest --tests io.github.samzhu.skillshub.score.QualityScoreListenerTest` PASS，證明 quality judge disabled 時 production classpath 仍有 deterministic stub bean 可啟動。
- 文件 scan PASS：`rg "Pattern 1|TestDataController|/internal/test|application-e2e" docs/grimo/PRD.md docs/grimo/architecture.md docs/grimo/development-standards.md docs/grimo/qa-strategy.md docs/grimo/test-cases.md docs/grimo/glossary.md docs/grimo/adr` 只剩 ADR-007 / ADR-008 的 superseded 或歷史說明。
- `./scripts/verify-all.sh` PASS：V01=PASS, V02=INFO line coverage 87.4%, V03=PASS, V04=PASS, V05=PASS, V06=PASS, V07=PASS, V08a=PASS, V08b=PASS；exit=0。

下一步：執行 `$verifying-quality S202`。
