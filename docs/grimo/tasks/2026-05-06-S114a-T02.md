# S114a-T02: Domain Model — Role, SkillGrant, Events, Repository, Skill.ownerId

## Spec
S114a — RBAC ACL with Materialized Projection (Owner + Viewer roles)

## BDD
Given: T01 已完成 V16/V17 migration（`skill_grants` 表存在，`skills.owner_id` NOT NULL）
When: 建立 `Role` enum、`SkillGrant` entity、`SkillGrantedEvent` / `SkillRevokedEvent` records、`SkillGrantRepository`，並在 `Skill` aggregate 加 `ownerId` 欄位
Then:
- `Role.OWNER.permissions()` 回 `["read","write","delete"]`
- `Role.VIEWER.permissions()` 回 `["read"]`
- `SkillGrant.create(skillId, principalType, principalId, role, grantedBy)` 生成 UUID id + 正確 fields
- `SkillGrantRepository.findBySkillId()` / `existsBySkillIdAndRole()` 可呼叫
- `Skill.getOwnerId()` 回 `owner_id` column 值
- 編譯通過（`./gradlew compileTestJava` PASS）

## Target Files
### New
- `backend/src/main/java/io/github/samzhu/skillshub/skill/security/Role.java`
- `backend/src/main/java/io/github/samzhu/skillshub/skill/security/SkillGrant.java`
- `backend/src/main/java/io/github/samzhu/skillshub/skill/security/SkillGrantRepository.java`
- `backend/src/main/java/io/github/samzhu/skillshub/skill/security/events/SkillGrantedEvent.java`
- `backend/src/main/java/io/github/samzhu/skillshub/skill/security/events/SkillRevokedEvent.java`

### Modified
- `backend/src/main/java/io/github/samzhu/skillshub/skill/domain/Skill.java` (加 `ownerId` field + `getOwnerId()`)

## Key Design Notes

### Role.java
```java
public enum Role {
    OWNER, VIEWER;
    public List<String> permissions() {
        return switch (this) {
            case OWNER  -> List.of("read", "write", "delete");
            case VIEWER -> List.of("read");
        };
    }
}
```

### SkillGrant.java (Spring Data JDBC entity, NOT AbstractAggregateRoot)
```java
@Table("skill_grants")
public class SkillGrant implements Persistable<String> {
    @Id private String id;
    @Column("skill_id") private String skillId;
    @Column("principal_type") private String principalType;
    @Column("principal_id") private String principalId;
    private String role;
    @Column("granted_by") private String grantedBy;
    @Column("granted_at") private Instant grantedAt;
    @Version @JsonIgnore private Long version; // NOT needed for SkillGrant — remove if causing issues
    // factory + isNew() + accessors
}
```
注意：`SkillGrant` 不發 domain events，不需 AbstractAggregateRoot。事件由 `SkillGrantService` 直接 `publishEvent()`。

### SkillGrantRepository.java
```java
public interface SkillGrantRepository extends CrudRepository<SkillGrant, String> {
    List<SkillGrant> findBySkillId(String skillId);
    boolean existsBySkillIdAndRole(String skillId, String role);
    Optional<SkillGrant> findBySkillIdAndPrincipalTypeAndPrincipalId(
        String skillId, String principalType, String principalId);
}
```

### Events
```java
// SkillGrantedEvent.java
public record SkillGrantedEvent(String skillId, String grantId) {}

// SkillRevokedEvent.java
public record SkillRevokedEvent(String skillId, String grantId) {}
```

### Skill.ownerId field
- 加在 `Skill.java`：`@Column("owner_id") private String ownerId;`
- 加 `@ReadOnlyProperty`（與 `downloadCount` / `riskLevel` 同 pattern — ownerId 只由 migration backfill 和 SkillAclProjectionListener 寫入，aggregate save 不應覆蓋）
- 加 `getOwnerId()` accessor

## Depends On
- T01

## Status
pending
