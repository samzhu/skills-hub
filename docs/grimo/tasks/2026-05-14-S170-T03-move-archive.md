# S170-T03: Move and archive semantics

## Spec
S170 — Group tree principal model

## BDD
Given `Acme -> Cloud -> Platform Team`
And Bob belongs to `Platform Team`
When admin moves `Cloud` under `Global`
Then closure rows are rebuilt and Bob's ancestor principals change
When admin archives `Cloud`
Then archived subtree Groups stop appearing in active principal context

## Target Files
- `backend/src/main/java/io/github/samzhu/skillshub/org/GroupService.java`
- `backend/src/main/java/io/github/samzhu/skillshub/org/GroupMovedEvent.java`
- `backend/src/main/java/io/github/samzhu/skillshub/org/GroupArchivedEvent.java`
- `backend/src/test/java/io/github/samzhu/skillshub/org/GroupServiceTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/org/PrincipalContextServiceTest.java`

## AC
- AC-7
- AC-11

## Depends On
- S170-T02

## Status
PASS

## Result

Implemented subtree move closure rebuild and archive semantics.

Files changed:

- `backend/src/main/java/io/github/samzhu/skillshub/org/GroupService.java`
- `backend/src/main/java/io/github/samzhu/skillshub/org/GroupMovedEvent.java`
- `backend/src/main/java/io/github/samzhu/skillshub/org/GroupArchivedEvent.java`
- `backend/src/main/java/io/github/samzhu/skillshub/shared/security/PrincipalContextService.java`
- `backend/src/test/java/io/github/samzhu/skillshub/org/GroupServiceTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/org/PrincipalContextServiceTest.java`

RED:

```bash
./gradlew test --tests "*GroupServiceTest" --tests "*PrincipalContextServiceTest"
```

Result: FAIL — `compileTestJava` failed because `GroupService.archiveGroup(String)` did not exist.

GREEN:

```bash
./gradlew test --tests "*GroupServiceTest" --tests "*PrincipalContextServiceTest"
```

Result: PASS — Gradle output ended with `BUILD SUCCESSFUL in 1m 47s`.

Notes:

- `GroupService.moveGroup(...)` now deletes old external ancestor closure rows for the moved subtree, keeps subtree-internal rows, and inserts new parent ancestor rows.
- `GroupService.archiveGroup(...)` marks the selected Group and all descendants as `ARCHIVED`.
- `PrincipalContextService.currentPrincipalKeys()` now requires both the direct membership Group and the returned ancestor Group to be `ACTIVE`, so archived subtree membership produces no Group principal.
- AOT note: `GroupService` keeps one Spring constructor; the manual collision test passes a no-op event publisher explicitly.
