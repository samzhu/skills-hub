# S114a-T04: SkillAclProjectionListener + AclPrincipalExpander company expansion

## Spec
S114a — RBAC ACL with Materialized Projection (Owner + Viewer roles)

## BDD
Given: T03 SkillGrantService 已完成；skill_grants 表有 OWNER / VIEWER grants
When:
1. alice 上傳新 skill（SkillCreatedEvent fired）
2. alice grant bob VIEWER (SkillGrantedEvent fired)
3. alice revoke bob VIEWER (SkillRevokedEvent fired)
4. listener 重複觸發同一 event（AC-10 幂等性）
5. bob token 帶 company_id="acme"（AC-9）
Then:
- AC-1: SkillCreatedEvent → listener auto-seed OWNER grant row + rebuildAcl → acl_entries 含 alice 三個 entries
- AC-2: SkillGrantedEvent → rebuildAcl → acl_entries 含 "user:bob:read"（polling ≤ 2s after commit）
- AC-3: grant public:* VIEWER → is_public = TRUE; acl_entries 含 "public:*:read"
- AC-4: SkillRevokedEvent → rebuildAcl → acl_entries 不含 "user:bob:read"
- AC-9: AclPrincipalExpander.expand(bob with companyId="acme", "read") → 包含 "company:acme:read"
- AC-10: 同 event 重跑 → acl_entries 結果不變（no duplicate entries）
- AC-12: `./gradlew modulithTest` PASS（無 Modulith cycle）
- `./gradlew test --tests SkillAclProjectionListenerTest` PASS
- `./gradlew test --tests AclPrincipalExpanderTest` PASS

## Target Files
### New
- `backend/src/main/java/io/github/samzhu/skillshub/skill/security/SkillAclProjectionListener.java`
- `backend/src/test/java/io/github/samzhu/skillshub/skill/security/SkillAclProjectionListenerTest.java`

### Modified
- `backend/src/main/java/io/github/samzhu/skillshub/shared/security/AclPrincipalExpander.java` (加 company: expansion)
- `backend/src/test/java/io/github/samzhu/skillshub/shared/security/AclPrincipalExpanderTest.java` (加 AC-9 test case)

## Key Design Notes

### SkillAclProjectionListener
```java
@Component
public class SkillAclProjectionListener {
    private final SkillGrantRepository grantRepo;
    private final NamedParameterJdbcTemplate jdbc;
    private final ObjectMapper mapper;

    // Auto-seed OWNER grant when skill is created
    @ApplicationModuleListener
    public void onSkillCreated(SkillCreatedEvent event) {
        if (event.authorId() == null) return; // null author = no owner (test fixture)
        // Upsert OWNER grant for author
        var existing = grantRepo.findBySkillIdAndPrincipalTypeAndPrincipalId(
            event.skillId(), "user", event.authorId());
        if (existing.isEmpty()) {
            grantRepo.save(SkillGrant.create(
                event.skillId(), "user", event.authorId(), Role.OWNER, event.authorId()));
        }
        rebuildAcl(event.skillId());
    }

    @ApplicationModuleListener
    public void onGranted(SkillGrantedEvent event) { rebuildAcl(event.skillId()); }

    @ApplicationModuleListener
    public void onRevoked(SkillRevokedEvent event) { rebuildAcl(event.skillId()); }

    private void rebuildAcl(String skillId) {
        // advisory lock 防 race
        jdbc.queryForList("SELECT pg_advisory_xact_lock(hashtext('acl:' || :id)::bigint)",
            Map.of("id", skillId));
        var grants = grantRepo.findBySkillId(skillId);
        var entries = grants.stream()
            .flatMap(g -> Role.valueOf(g.role()).permissions().stream()
                .map(perm -> g.principalType() + ":" + g.principalId() + ":" + perm))
            .distinct()
            .toList();
        try {
            var json = mapper.writeValueAsString(entries);
            jdbc.update("UPDATE skills SET acl_entries = :acl::jsonb WHERE id = :id",
                Map.of("id", skillId, "acl", json));
        } catch (Exception e) {
            throw new RuntimeException("Failed to rebuild ACL for " + skillId, e);
        }
    }
}
```

### AclPrincipalExpander — 加 company expansion
```java
// 在 expand() 方法的 user + role + group 之後，read 之前，加：
if (user.companyId() != null && !user.companyId().isBlank()) {
    patterns.add("company:" + user.companyId() + ":" + permission);
}
```

注意：`CurrentUser` record 需確認有 `companyId()` accessor（既有 `shared/security/CurrentUser.java`）。

### Modulith 邊界注意
- `SkillAclProjectionListener` 訂閱 `SkillCreatedEvent`（`skill.domain`）、`SkillGrantedEvent`（`skill.security`）、`SkillRevokedEvent`（`skill.security`）
- 三者均在同一 `skill` module → 無跨模組 dependency → Modulith test 不受影響 ✓

### SkillAclProjectionListenerTest 策略
使用 `@SpringBootTest` + `@Import(TestcontainersConfiguration.class)` + `@EnableScenarios` + `Scenario` API（與 S135a QualityScoreListenerTest 同模式）：
```java
scenario.publish(new SkillGrantedEvent(skillId, grantId))
    .andWaitAtMost(Duration.ofSeconds(5))
    .andWaitForStateChange(() -> loadAclEntries(skillId))
    .andVerify(entries -> assertThat(entries).contains("user:bob:read"));
```

## Depends On
- T03

## Status
pending
