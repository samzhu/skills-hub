# S140-T07: E2E spec — Semantic search (P5)

## Spec
S140 — E2E Critical Path Backfill (AC-5)

## BDD
Given Platform seeded with 5 skills (`profiles.paged`, 含 docker / k8s / testing / lint / docs 五類)
And   Backend running under `e2e` profile → `E2EEmbeddingConfig.e2eStubEmbeddingModel` (`@Primary`) takes precedence over `googleGenAiEmbeddingModel` / `noOpEmbeddingModel`
When  User opens /search, enters query "我想把應用部署到容器環境", submits
Then  Result list is non-empty (5 skills returned, e2e stub embedder all > -0.1 cosine → all included)
And   Each result shows match reason field
And   Reload page (page.reload + same query) → result order is identical (deterministic verification — POC validated H3)
And   Test tagged `@S140 @ac-5 @happy-path @profile-paged`
And   Authored via `/playwright-expert` DESIGN mode

## Notes
- AC-5 deliberately does NOT assert "docker first" — POC (poc/S140/StubEmbeddingPoc.java) showed Random(hashCode) puts docker LOWEST (-0.058) for the container-deploy query. AC verifies route + render + determinism, not semantic quality.
- 真 Gemini 排序質量由 backend integration test + LAB 部署驗證涵蓋

## Target Files
- `e2e/tests/S140-critical-path-semantic-search.spec.ts` (new)

## Depends On
- S140-T02

## Status
pending
