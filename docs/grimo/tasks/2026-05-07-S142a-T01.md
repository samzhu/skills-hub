# S142a-T01: Foundation — Types / API / Hooks + shiki POC + bundle gate

## Spec
S142a — SkillDetailPage v2 Frontend Rework

## BDD

Given S142b API contract (spec §4.2-4.4)
When  frontend types / api functions 對齊 backend response shape
Then  `frontend/src/types/skill.ts` Skill interface 加 6 fields:
  - `verified: boolean`
  - `latestVersionPublishedAt: string | null` (ISO 8601)
  - `license: string | null`
  - `compatibility: string[]`
  - `versionCount: number`
  - `openFlagCount: number`
And `frontend/src/api/scores.ts` SkillScores interface 加 `skillScore: number | null`
And `frontend/src/api/security.ts` 新建：SecurityCheck / SecurityReport types + `fetchSecurityReport(skillId): Promise<SecurityReport | null>` (404 → null)
And `frontend/src/hooks/useSkillScores.ts` return type 多 skillScore (從 extended response 取)
And `frontend/src/hooks/useSecurityReport.ts` 新建（React Query hook，staleTime 60_000；404 → null fallback）

Given shiki dependency 加進 frontend
When  npm install shiki + 簡單 POC component (load 1 lang, highlight code block)
Then  build 跑成功
And   `npm run build` bundle delta 量出（gzip 增加 N KB）
And   如 N <= 50 KB → adopt shiki
And   如 N > 50 KB → fallback prism-react-renderer + record decision in spec §7

Given API contract 對齊
When  既有 frontend test 跑（`npm test`）
Then  全 PASS（純 type addition / new files；既有 caller 不破）

## Target Files
- frontend/src/types/skill.ts [MODIFIED — Skill interface 加 6 fields]
- frontend/src/api/scores.ts [MODIFIED — SkillScores 加 skillScore]
- frontend/src/api/security.ts [NEW — SecurityCheck / SecurityReport types + fetchSecurityReport()]
- frontend/src/hooks/useSkillScores.ts [MODIFIED — return type 多 skillScore]
- frontend/src/hooks/useSecurityReport.ts [NEW — React Query hook]
- frontend/package.json [MODIFIED — 加 shiki dep (or prism-react-renderer if bundle 爆)]
- frontend/src/lib/syntax-highlighter.ts [NEW — shiki wrapper / lang detection from file extension]

## AC Coverage
- (foundation — no direct AC mapping; unblocks T02-T06 to consume API + use highlighter)

## Depends On
- S142b API contract (frozen via S142b spec §4.2-4.4；S142b 不需 ship 完成，只需 spec lock)

## POC Note
Shiki bundle size = Hypothesis per spec §2.6。POC 在 T01 內 inline 驗：
1. `npm install shiki` + lazy import
2. minimal usage in lib/syntax-highlighter.ts
3. `npm run build` → record dist size delta
4. 如 > 50 KB gzip → switch to prism-react-renderer，重做 wrapper

## Status
pending
