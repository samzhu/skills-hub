# S202-T04: Production API seed helper and browser fixture migration

## 對應規格
S202：Production E2E Fixture Runner

## 這個 task 要做什麼
這個 task 完成後，`seedSkill()` 不再打 `/internal/test/seed/skill`，而是用 mock OAuth Bearer token 呼叫正式 `POST /api/v1/skills/upload`。現有 browser tests 會讀 `e2e/results/fixtures.json` 的 shared baseline，或用 production API helper 建 per-test unique skill。`rg '/internal/test' e2e/tests e2e/fixtures` 只允許 setup route absence check。

## 使用者情境（BDD）
Given（前提）mock OAuth server 可發 `developer-client` token，且 app 的 OAuth Resource Server 已啟用
When（動作）setup fixtures 呼叫 `uploadSkillFixture({ asUser: "developer", name: "docker-compose-helper" })`
Then（結果）正式 API `POST /api/v1/skills/upload` 回 201 與 `{ "id": "<uuid>" }`
And（而且）`e2e/results/fixtures.json` 有 `profiles.single.skill.id` 與 `profiles.single.skill.detailPath`
When（動作）執行 `rg '/internal/test' e2e/tests e2e/fixtures`
Then（結果）只允許 setup route absence check 檔案出現 `/internal/test/reset`
And（而且）任何 caller 都不能用 request body/query param override author

## 研究來源
- `backend/src/main/java/io/github/samzhu/skillshub/skill/command/SkillCommandController.java:95-127`
- `backend/src/main/java/io/github/samzhu/skillshub/shared/security/CurrentUserProvider.java:82-130`
- `backend/config/oauth-mock-config.json`
- `e2e/tests/_fixtures.ts`
- `docs/grimo/specs/2026-05-19-S202-production-e2e-fixture-runner.md` §2.6 / §2.7 / §4.3

## 先做 POC
- POC：required — 需要證明 production upload API 可用 mock OAuth token seed skill。
- Fixture：
  - `positive-fixture`: `developer-client` token + valid skill zip → 201 + manifest id
  - `negative-fixture`: no token 或 bad token → 401/403，setup fail with readable message
- POC 跑完必須印出 `S202 T04 POC PASS`。

## 正式程式怎麼做
- 新增 `e2e/fixtures/production-api-seed.ts`
  - `tokenFor(role: "developer" | "viewer" | "admin")`
  - `uploadSkillFixture(request, input)`
  - 建立 minimal skill zip；zip 內容至少包含 `SKILL.md`
  - multipart fields 對齊 production API：`file`、`skillName`、`version`、`category`、`visibility`
  - 不傳 `author`
- 修改 `e2e/fixtures/setup.fixtures.ts`
  - seed `profiles.single.skill`
  - seed `profiles.paged.skills[]`
  - manifest names 保留：
    - `docker-compose-helper`
    - `docker-image-builder`
    - `docker-cleaner`
- 修改 `e2e/tests/_fixtures.ts`
  - 移除 `TEST_API_BASE`
  - 移除 auto reset fixture
  - export `readManifest()` / `fixtureSkill()` / production `seedSkill()`
- 修改 `e2e/tests/*.spec.ts`
  - shared read-only baseline 改讀 manifest
  - mutable/ad-hoc data 改 `seedSkill({ asUser })`

## 單元測試 / 整合測試
- `e2e/fixtures/production-api-seed.spec.ts`
  - `@S202 @AC-S202-4: uploads skill through production API with developer token`
  - `@S202 @AC-S202-5: seed helper never calls /internal/test`
- Existing happy-path specs must still run with `@happy-path`

## 會改哪些檔案
- `e2e/fixtures/production-api-seed.ts`
- `e2e/fixtures/setup.fixtures.ts`
- `e2e/fixtures/manifest.ts`
- `e2e/tests/_fixtures.ts`
- `e2e/tests/*.spec.ts`

## 驗證方式
執行：`cd e2e && npx playwright test --project "setup fixtures" && rg '/internal/test' e2e/tests e2e/fixtures`

## 前置條件
- S202-T03 PASS

## 狀態
PASS

## Result

日期：2026-05-19

### RED

- `cd e2e && npx playwright test --project "fixture unit" --grep "@AC-S202-4"`：FAIL — `Error: No tests found`，代表 production API seed helper 的驗證尚未存在。
- `rg -n '/internal/test' e2e/tests e2e/fixtures`：FAIL for T04 scope — `e2e/tests/_fixtures.ts` 仍含 `TEST_API_BASE = 'http://localhost:8080/internal/test'`，且 `seedSkill()` / `seedDownloadEvents()` 仍呼叫 `/internal/test/*`。

### GREEN

- `cd e2e && npx playwright test --project "fixture unit" --grep "@AC-S202-4|@AC-S202-5"`：PASS — 3 passed（含 teardown）；`uploadSkillFixture()` 以 `developer-client` token 打正式 `POST /api/v1/skills/upload`，回傳 UUID。
- `cd e2e && npx playwright test --project "setup fixtures"`：PASS — 2 passed；`e2e/results/fixtures.json` 由正式 upload API 寫出 `profiles.single.skill.id`、`profiles.single.skill.detailPath`、`profiles.paged.byName["docker-compose-helper"].id`，console 印出 `S202 T04 POC PASS`。
- `rg -n '/internal/test' e2e/tests e2e/fixtures`：PASS — 只剩 `e2e/fixtures/setup.fixtures.ts` 的 route absence check：`POST /internal/test/reset` 必須回 404/405。
- `cd e2e && npx playwright test --project "fixture unit"`：PASS — 5 passed（DB guard + production API seed helper + teardown）。
- `cd e2e && npx playwright test --list`：PASS — Playwright 列出 setup/teardown/fixture unit/chromium tests，新增 TS 檔可被載入。
- `git diff --check`：PASS — 無 whitespace error。
- `cd e2e && docker compose -f compose.e2e.yaml ps --all`：PASS — 無殘留 container。

### Files Changed

- `e2e/fixtures/production-api-seed.ts`：新增 `tokenFor()`、`uploadSkillFixture()`、minimal zip builder；multipart 只送 `file`、`skillName`、`version`、`category`、`visibility`，不送 caller-supplied `author`。
- `e2e/fixtures/production-api-seed.spec.ts`：新增 `@S202 @AC-S202-4` production upload test 與 `@S202 @AC-S202-5` forbidden route source check。
- `e2e/fixtures/manifest.ts`：擴充 manifest schema，支援 `profiles.single.skill`、`profiles.paged.skills[]`、`profiles.paged.byName` 與 `detailPath`。
- `e2e/fixtures/setup.fixtures.ts`：reset disposable DB 後，用 production API seed shared baseline skills，保留 `docker-compose-helper`、`docker-image-builder`、`docker-cleaner` names。
- `e2e/tests/_fixtures.ts`：移除 auto reset 與 `/internal/test` seed path；`profiles.single()` / `profiles.paged()` 改讀 manifest；ad-hoc `seedSkill()` 改走 production API helper。

### Notes

- Playwright API request multipart 依據：[Playwright API testing](https://playwright.dev/docs/api-testing)。
- `seedDownloadEvents()` 暫時改成明確 fail-fast，因 projection direct SQL helper 屬於 S202-T05 範圍；這避免 T04 留下 `/internal/test/seed/download-event` 的半舊路徑。
