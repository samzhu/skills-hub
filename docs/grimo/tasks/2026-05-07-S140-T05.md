# S140-T05: E2E spec — Upload + publish flow (P2 + P3)

## Spec
S140 — E2E Critical Path Backfill (AC-3)

## BDD
Given Empty platform state (`profiles.empty`)
And   User session preloaded as authenticated (Playwright `storageState` 模擬已 OAuth login per S139)
When  User opens /publish, uploads zip containing only SKILL.md (no scripts/), submits
Then  Page redirects to /publish/review showing "上傳成功"
And   Skill enters "已上架" state
And   Risk level = "低風險"
And   Version v1.0.0 created
And   Test tagged `@S140 @ac-3 @happy-path @profile-empty`
And   Authored via `/playwright-expert` DESIGN mode

## Notes
- High-risk path (含 scripts/) covered by backend integration test, NOT this E2E
- Authenticated state via `storageState` (NOT real OAuth round-trip in test) — see playwright-expert auth notes

## Target Files
- `e2e/tests/S140-critical-path-publish.spec.ts` (new)

## Depends On
- S140-T02

## Status
pending
