# S142b-T03: SkillScoreCalculator + extend ScoreResponse + QualityScoreController integration

## Spec
S142b — SkillDetailPage v2 Backend Supplement

## BDD

Given qualityTotal=92 + securityScore=100
When  SkillScoreCalculator.compute(qualityTotal, securityScore)
Then  return round(0.6 × 92 + 0.4 × 100) = round(55.2 + 40) = 95

Given qualityTotal=85 + securityScore=100
When  compute
Then  return round(0.6 × 85 + 0.4 × 100) = round(51 + 40) = 91

Given qualityTotal=92 + securityScore=75 (1 warn)
When  compute
Then  return round(0.6 × 92 + 0.4 × 75) = round(55.2 + 30) = 85

Given qualityTotal=92 + securityScore=null (未掃描)
When  compute
Then  return null

Given skill 已評分 + 已掃描 4 quad 全 pass
When  GET /api/v1/skills/{id}/scores
Then  200 OK
And   response 含 既有 fields (validation / implementation / activation / total)
And   response 多 1 field skillScore = round(0.6 × total + 0.4 × 100)
And   既有 caller test 不破（field addition only）

Given skill 剛 publish 還沒評 quality（Quality 404）
When  GET /api/v1/skills/{id}/scores
Then  404 QUALITY_NOT_EVALUATED（既有行為，不變）

Given skill 已評 quality 但 security 未掃描
When  GET /api/v1/skills/{id}/scores
Then  200 OK
And   既有 quality fields 都正常
And   skillScore = null（security score 不存在 → 整個 composite null）

## Target Files
- backend/src/main/java/io/github/samzhu/skillshub/score/SkillScoreCalculator.java [NEW]
- backend/src/main/java/io/github/samzhu/skillshub/score/ScoreResponse.java [MODIFIED — 加 `Integer skillScore` field]
- backend/src/main/java/io/github/samzhu/skillshub/score/QualityScoreController.java [MODIFIED — 注入 SkillScoreCalculator + SecurityCategoryMapper, compute skillScore]
- backend/src/test/java/io/github/samzhu/skillshub/score/SkillScoreCalculatorTest.java [NEW] (pure unit)
- backend/src/test/java/io/github/samzhu/skillshub/score/QualityScoreControllerTest.java [MODIFIED — assert skillScore field]

## AC Coverage
- AC-S142b-4 (skillScore composite in /scores response)
- AC-S142b-5 (Quality 404 → no skillScore — protected by existing 404 path)

## Depends On
- S142b-T01 (SecurityCategoryMapper for securityScore lookup)

## Status
pending
