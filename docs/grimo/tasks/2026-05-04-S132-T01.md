# S132-T01: Local atomic edit — cloudbuild.yaml + Gradle 解耦 + script 自帶 frontend build + 三份 docs sync

## Spec
S132 — CI Cloud Build pipeline (build + push image)

## BDD

```gherkin
Given backend/build.gradle.kts 目前透過 npmInstall / npmBuild / copyFrontend
      三 task 把 frontend npm build 串進 processResources → bootRun / bootJar /
      bootBuildImage 都會觸發 npm，導致 frontend TS 錯誤擋住 backend 啟動

When  按 spec §4 落地：
        1. 新增 cloudbuild.yaml 於 repo root（§4.1 三 step + declarative images:）
        2. 改 backend/build.gradle.kts：刪 npmInstall / npmBuild / copyFrontend；
           processResources 只留 config/ yaml 拷貝（§4.2）
        3. 改 scripts/gcp/03-build-push.sh：line 24 之前加 npm ci + npm run build + cp
           三步前置（§4.3），讓本機 path 自帶 frontend build
        4. Sync docs：
           - scripts/gcp/README.md：加「CI Cloud Build trigger」段
           - scripts/gcp/DEPLOYMENT.md：build/push 段註明「or wait for Cloud Build」
           - docs/grimo/architecture.md lines 415-426：Build Integration 段更新成
             兩條路徑（local dev / CI），Gradle 不再 invoke npm

Then  AC-1 cloudbuild.yaml 通過 Cloud Build schema 驗證（gcloud builds submit
      --config=cloudbuild.yaml --no-source --substitutions=... 不報 schema error）
  And AC-2 cd backend && ./gradlew bootRun 不執行 :npmInstall / :npmBuild
      （task 不存在），Spring Boot 正常啟動，frontend TS 錯誤不影響後端
  And AC-6 source scripts/gcp/.env && ./scripts/gcp/03-build-push.sh 仍可本機跑
      （exit 0；frontend dist 自動 build；image 推到 AR 同 path / SHA + :latest 雙 tag）
```

## Target Files

**新增**
- `cloudbuild.yaml`（repo root）— per spec §4.1

**修改**
- `backend/build.gradle.kts`（刪 lines 103-127；簡化 lines 129-137 processResources）
- `scripts/gcp/03-build-push.sh`（line 24 之前插入 npm ci + npm run build + cp 三步）
- `scripts/gcp/README.md`（加 CI Cloud Build trigger 段；指向 spec §4.4 setup 指令；說明 local script vs CI 兩路徑同 image / 同 SHA tag、deploy 仍手動）
- `scripts/gcp/DEPLOYMENT.md`（build/push 段補「or wait for Cloud Build to push automatically」；deploy 段不變）
- `docs/grimo/architecture.md`（lines 415-426 Build Integration 段更新；改成兩條路徑 ASCII；明示 Gradle 不再 invoke npm）

## Verification（local，無需 GCP credentials）

1. `cd backend && ./gradlew tasks --all 2>&1 | grep -E "npmInstall|npmBuild|copyFrontend"` → 空輸出（task 不存在）
2. `cd backend && ./gradlew bootRun` → 不跑 npm；Spring Boot 啟動到 "Started SkillshubApplication"（手動 Ctrl+C 結束）— **AC-2**
3. `( cd frontend && npm run build && cd .. && ls frontend/dist )` → 確認 frontend 仍可獨立 build
4. `bash -n scripts/gcp/03-build-push.sh` → 0；`shellcheck scripts/gcp/03-build-push.sh`（如有裝）warnings 控制在 既有等級或更少
5. `source scripts/gcp/.env && ./scripts/gcp/03-build-push.sh` → exit 0；本機 docker images 看到 `<region>-docker.pkg.dev/<proj>/skillshub/skillshub:<sha>` + `:latest` — **AC-6**（**需要 Docker daemon + GCP credentials；若 user 環境不便驗證可標 deferred-manual**）
6. `gcloud builds submit --config=cloudbuild.yaml --no-source --substitutions=_GCP_PROJECT_ID=$GCP_PROJECT_ID,_REGION=$GCP_REGION` → CLI 接受 YAML schema（step 因 no-source 立即失敗屬預期）— **AC-1**（**需要 gcloud auth 與 cloudbuild API enabled；若 user 環境未備可延 T02 一起驗**）

## Depends On
none

## Status
PASS

## Result
Date: 2026-05-04
Test: config-absent → config-present + task-not-registered checks（infra task；無 JUnit）
Files changed:
- `cloudbuild.yaml` (new) — repo root；3 step pipeline + declarative `images:` push
- `backend/build.gradle.kts` (modified) — 刪 `npmInstall` / `npmBuild` / `copyFrontend` 三 task；`processResources` 只留 `config/` yaml 拷貝（保留 `application-prod.yaml` / `application-lab.yaml` include block）
- `scripts/gcp/03-build-push.sh` (modified) — line 24 之前插入 `npm ci && npm run build` + `cp -r frontend/dist/. backend/src/main/resources/static/` 三步前置
- `scripts/gcp/README.md` (modified) — 加「CI Cloud Build trigger」段（一次性 setup + 觸發後驗證 + deploy 該 SHA）
- `scripts/gcp/DEPLOYMENT.md` (modified) — Step 9 開頭加「Or wait for Cloud Build to push automatically」note；失敗 troubleshoot 補 S132 變化
- `docs/grimo/architecture.md` (modified) — Build Integration 段更新成兩條路徑（local dev 並行 / CI 串接），明示 Gradle 不 invoke npm

### RED → GREEN 驗證命令

| Check | RED 輸出 | GREEN 輸出 |
|---|---|---|
| `grep -nE "npmInstall\|npmBuild\|copyFrontend\|frontendDir" backend/build.gradle.kts` | 11 行 hits（lines 103-130） | exit 1 無輸出 |
| `ls cloudbuild.yaml` | `No such file or directory` | `cloudbuild.yaml` |
| `bash -n scripts/gcp/03-build-push.sh` | `bash syntax OK`（語法本來就過） | `bash syntax OK` |
| `cd backend && ./gradlew tasks --all 2>&1 \| grep -E "npmInstall\|npmBuild\|copyFrontend"` | 三 task 列出於 "Other tasks" | exit 1 無輸出 — **AC-2 verified at task-graph level** |
| `grep -nE "Build frontend\|npm ci\|cp -r frontend" scripts/gcp/03-build-push.sh` | 0 hits | 3 hits（lines 24 / 27 / 32）— AC-6 verified at script-content level |

### Deferred-manual（需要 GCP credentials / Docker daemon，留 T02 一起跑）

- AC-1 `gcloud builds submit --config=cloudbuild.yaml --no-source --substitutions=_GCP_PROJECT_ID=...,_REGION=...` schema 接受 — 跨 T02 setup 階段一併觸發第一次 build 時驗證
- AC-2 完整 `./gradlew bootRun` 跑到 Spring Boot "Started SkillshubApplication" 訊息 — 需要 Docker daemon（spring-boot-docker-compose 拉 pgvector + grafana），task-graph level 已驗
- AC-6 完整 `source scripts/gcp/.env && ./scripts/gcp/03-build-push.sh` 跑完 + AR 收 SHA + :latest 雙 tag — 需要 Docker daemon + GCP credentials；建議與 T02 首次 push 對照驗證（同 SHA 兩條路徑同 image digest）

Notes: —
