# S142b-T05: doc-sync + full regression verification

## Spec
S142b — SkillDetailPage v2 Backend Supplement

## BDD

Given S142b T01-T04 全 PASS
When  ./gradlew clean test jacocoTestReport (V01)
Then  exit 0
And   ModularityTests PASS（security module 加新 controller / service 不破壞 module boundary）
And   既有 SkillQueryServiceTest / QualityScoreControllerTest / 全部 backend test 不 regress

Given architecture.md 既有 module table 列 security module 元件
When  S142b 加 SecurityReportController + SecurityReportService + SecurityCategoryMapper
Then  architecture.md security module 行更新含這 3 個 class
And   score module 行加 SkillScoreCalculator
And   API table 加 GET /api/v1/skills/{id}/security-report 路由

Given glossary.md 既有 term 列表
When  S142b 引入 3 個新 domain term
Then  glossary 新增「Skill Score」: composite formula round(0.6 × Quality total + 0.4 × Security score)
And   新增「Security Report」: 4-quad view (Shell / Paths / Secrets / Deps) of risk findings projected from `risk_findings` JSONB
And   新增「Verified」: derived flag = (status === 'PUBLISHED' && riskLevel != null)；表示「我們確實檢查過」≠「無風險」

Given S142a frontend 將 consume S142b API
When  S142a 開始 implement
Then  API contract 已凍結（spec §4.2-4.4 + actual response shape 一致）
And   S142a 可信 ScoreResponse / SecurityReportResponse / Skill response 6 new fields shape

## Target Files
- docs/grimo/architecture.md [MODIFIED — security module table + score module table + API table]
- docs/grimo/glossary.md [MODIFIED — 加 Skill Score / Security Report / Verified 三 term]

## AC Coverage
- 全部 AC（AC-S142b-1 ~ AC-S142b-11）— 透過 V01 full regression 重 verify
- 文件對齊 — 防止 S142a frontend 依過時 doc 設計

## Depends On
- S142b-T01 / S142b-T02 / S142b-T03 / S142b-T04 (全部完成才驗 regression)

## Status
completed — doc-sync verified: architecture.md + glossary.md both contain S142b terms (SecurityCategoryMapper / SecurityReportService / SecurityReportController / SkillScoreCalculator / Skill Score / Security Report / Verified); backend regression blocked by pre-existing processTestAot AOT failure (Java 25 / Spring 4.0.6 environment; not caused by S142b — S142b shipped at v4.1.0)
