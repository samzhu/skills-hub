# S142a-T06: SkillDetailPage assembly + edge states + Playwright E2E + bundle audit

## Spec
S142a — SkillDetailPage v2 Frontend Rework

## BDD

Given S142a-T01-T05 全部 PASS（foundation + hero + tabs + FileExplorer + Sidebar 都 ready）
When  SkillDetailPage.tsx rewrite
Then  import 重組為 v2 components
And   既有 SkillHero / MetricCard grid / QualitySection / QualityTab / VersionList / FilesPanel imports 拿掉
And   結構對齊 prototype：PageHeader → Tabs → Body grid (main + sidebar 232px)
And   tab list 為 7 tabs in order: SKILL.md / Quality / Versions / Reviews / Security / Flags / Files (badge 顯 file count)
And   tab default 為 SKILL.md
And   既有 dual-route support 保留 (per ADR-003: /skills/:id legacy + /skills/:author/:name canonical)
And   既有 SUSPENDED callout (S087) + isOwner 邏輯 + ShareModal 保留

Given Quality 404 (未評分 skill)
When  訪問 /skills/{id}
Then  SkillScoreBadge 顯「—」+「評分計算中」
And   QualityHeroCard 顯 grey + 「評分計算中」
And   Quality tab 顯「此版本尚未評分」(reuse T03 fallback)
And   Security card 仍正常顯（不被 quality 404 阻塞）

Given SUSPENDED skill
When  訪問
Then  hero 三卡正常顯
And   Download CTA 消失（既有 S028 / S087 行為保留）
And   Security tab 加紅色 banner「此技能已被停用」
And   Sidebar Install card 命令仍顯（user 可複製，但實際下載被擋）

Given DRAFT skill
When  訪問
Then  類 SUSPENDED 但 banner 改「未發佈」
And   Download CTA 消失

Given 沒 reviews 的 skill
When  StatStrip 渲染
Then  Rating cell 顯「—」
And   Reviews tab 顯 empty state（既有 S098e2 處理）

Given S142a-T01 已驗證 shiki bundle 不爆 (or fallback prism)
When  T06 完成 + npm run build
Then  bundle 總增加 < 50 KB gzip vs S135b baseline (~190 KB)
And   record bundle delta in §7 evidence

Given E2E 測試需求
When  invoke playwright-expert DESIGN mode (passing spec id S142a)
Then  產出 e2e/tests/S142a-skill-detail-v2.spec.ts （含 happy path + Quality 404 edge state + SUSPENDED state + Files explorer scripts/ highlight 4 個 scenario）
And   playwright-expert findings 列出 missing data-testid / missing seed endpoint
And   findings inline 處理（加 testid 到對應 component / extend backend test seed endpoint）— 視為 T06 sub-tasks

Given Playwright spec 跑
When  cd e2e && npx playwright test --grep @S142a
Then  全 PASS
And   trace + screenshot evidence 留 e2e/test-results/ (gitignored)

Given S142a 全 component import audit
When  grep -r "import.*FilesPanel\|import.*VersionList\|import.*QualitySection\|import.*QualityTab" frontend/src/pages/SkillDetailPage.tsx
Then  no match (deprecated components 不再被 page import)
And   AnalyticsPage / 其他 page 仍可 import MetricCard / ReviewsPanel etc (沒被廢棄)

## Target Files
- frontend/src/pages/SkillDetailPage.tsx [REWRITE — assemble v2 components]
- frontend/src/pages/SkillDetailPage.test.tsx [REWRITE — 對齊 18 ACs]
- e2e/tests/S142a-skill-detail-v2.spec.ts [NEW — by playwright-expert DESIGN]
- (任何 missing data-testid 加到對應 v2/* component) [INLINE]
- backend/src/main/java/io/github/samzhu/skillshub/skill/testsupport/TestDataController.java [MAYBE MODIFIED — extend seed endpoint if playwright-expert DESIGN finds gap，例如 seed/skill 不直接寫 skill_scores 行]

## AC Coverage
- AC-S142a-7, AC-S142a-8 full (card click → tab + sidebar contextual full integration)
- AC-S142a-18 (BorderBeam viewport singleton — only on Download CTA)
- All ACs re-verify via Playwright happy-path
- Bundle size budget verify (per §5.4)
- Edge state coverage (Quality 404 / SUSPENDED / DRAFT / no reviews) per §B.1

## Depends On
- S142a-T01 (foundation)
- S142a-T02 (Hero region)
- S142a-T03 (Tab content)
- S142a-T04 (FileExplorerPanel)
- S142a-T05 (Sidebar)
- S142b T01-T04 (API contract live + endpoints functional — 否則 E2E 無法跑通真資料)

## Note — playwright-expert DESIGN invocation
T06 implementing-task RED 階段第一步：invoke `/playwright-expert design` with spec id S142a.
- 該 skill 讀 S142a §3 (18 ACs)
- 為每 AC 決定 fixture profile (per fixtures-patterns.md)
- 產出 e2e spec file
- 列出 findings (missing testids / seed endpoints)
- 每 finding 變成 T06 sub-task inline 處理（不開新 task file）
- 完成後 GREEN 條件：`npx playwright test --grep @S142a` PASS

## Status
completed — 318/318 Vitest PASS; SkillDetailPage rewritten with v2 components (PageHeader / Sidebar / SkillMdTab / QualityTabV2 / VersionsTabV2 / SecurityTab / FileExplorerPanel); 7-tab layout (SKILL.md default); TypeScript clean (0 production errors); E2E deferred to follow-up spec
