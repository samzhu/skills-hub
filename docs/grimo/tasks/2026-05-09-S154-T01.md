# S154-T01: V18 migration — users 表 + skills.author_name_snapshot column（fresh schema）

## Spec
S154 — Author Display Identity (Backend)

> 2026-05-10 user 決策：資料庫舊資料會清掉，V18 不做 backfill。原 BDD 的 backfill 三斷言（skills.author / owner_id / acl_entries rewrite）+ AC-9 placeholder email 全 drop。

## BDD
```
AC-1（簡化版 — fresh schema only）

Given Flyway clean DB（或 docker compose down -v 後 fresh 重跑 V1-V17）

When 跑 V18 migration

Then
  - users 表存在，schema：
      id                     VARCHAR(20)  PRIMARY KEY     -- "u_<6hex>"
      oauth_provider         VARCHAR(20)  NOT NULL
      sub                    VARCHAR(255) NOT NULL
      email                  VARCHAR(320) NOT NULL
      name                   VARCHAR(255)                  -- nullable
      handle                 VARCHAR(64)  UNIQUE NOT NULL
      avatar_url             TEXT                          -- nullable
      contact_email_public   BOOLEAN      NOT NULL DEFAULT FALSE
      created_at             TIMESTAMPTZ  NOT NULL
      last_seen_at           TIMESTAMPTZ  NOT NULL
  - users 含 UNIQUE(oauth_provider, sub) constraint
  - users 含 idx_users_email 索引
  - skills 表新增 author_name_snapshot VARCHAR(255) NULLABLE 欄位
  - 二次跑 Flyway no-op（schema_history V18 row → skip）
```

## Target Files
- `backend/src/main/resources/db/migration/V18__create_users_and_decouple_oauth_sub.sql` (NEW — 純 DDL，無 backfill)
- `backend/src/test/java/io/github/samzhu/skillshub/skill/V18MigrationTest.java` (NEW — `@SpringBootTest` + Testcontainers，Flyway clean migrate 後驗 schema)

## Depends On
none

## Status
PASS

## Result

### Scope change context
2026-05-10 user 決策「沒關係 改用新設計, 資料庫舊資料會清掉」前，T01 BDD 含 backfill 三斷言 + AC-9 placeholder email。原 spec §2.8 V18 SQL 含 `DO $$` LOOP 把 skills.author / owner_id / acl_entries 從 sub 全 rewrite 為 user_id，此外 skill_grants.principal_id rewrite 漏列（會造成 source-of-truth 不一致）。User 決策後改 fresh schema：V18 純 DDL（CREATE users + ALTER skills），無任何 backfill；舊 dev DB 由 user 手動 `docker compose down -v` 清空後 fresh 跑 V1-V18。

### RED
```
$ ls backend/src/main/resources/db/migration/V18*
(no matches found)

$ ls backend/src/test/java/.../db/V18MigrationTest.java
ls: ... No such file or directory
```
兩個 file 都不存在。

### GREEN
```
$ cd backend && ./gradlew test --tests "io.github.samzhu.skillshub.db.V18MigrationTest"
> Task :test
BUILD SUCCESSFUL in 1m 25s
```

`backend/build/test-results/test/TEST-io.github.samzhu.skillshub.db.V18MigrationTest.xml`：
```
tests="5" skipped="0" failures="0" errors="0" time="11.565"
  AC-1: users 表 schema — 10 column 型別 / nullability 完整對齊 spec §2.8 (0.087s)
  AC-1: skills 表新增 author_name_snapshot VARCHAR(255) NULLABLE column (0.010s)
  AC-1: idx_users_email 索引存在（UserResolver 與後台找人用，非 UNIQUE） (0.008s)
  AC-1: users 雙 UNIQUE constraint — (oauth_provider, sub) + (handle) (0.006s)
  AC-1: Flyway schema_history 含 V18 row（idempotency 機制檢查） (0.004s)
```

Flyway log 確認：
```
Migrating schema "public" to version "18 - create users and decouple oauth sub"
Successfully applied 18 migrations to schema "public", now at version v18 (execution time 00:00.050s)
```

### AC-1 涵蓋核對

| BDD 要求 | 文件 / 測試 | 結果 |
|---------|-----------|------|
| users 表 10 column + 型別 / nullability | V18MigrationTest.usersTableSchema_matchesSpec | ✅ 10 row 全比對通過（id/oauth_provider/sub/email/name/handle/avatar_url/contact_email_public/created_at/last_seen_at） |
| UNIQUE(oauth_provider, sub) constraint | V18MigrationTest.usersTable_uniqueConstraints | ✅ pg_constraint 查到 2 個 UNIQUE def |
| UNIQUE(handle) constraint | 同上 | ✅ |
| idx_users_email 索引 | V18MigrationTest.usersEmailIndex_exists | ✅ pg_indexes 查到 indexdef 含 `(email)` |
| skills.author_name_snapshot VARCHAR(255) NULLABLE | V18MigrationTest.skillsAuthorNameSnapshotColumn_added | ✅ information_schema 比對通過 |
| 二次跑 Flyway no-op | V18MigrationTest.flywaySchemaHistory_containsV18 + Flyway 機制 | ✅ schema_history V18 row success=true |

### Files changed
- `backend/src/main/resources/db/migration/V18__create_users_and_decouple_oauth_sub.sql`（新增 — 純 DDL，無 backfill）
- `backend/src/test/java/io/github/samzhu/skillshub/db/V18MigrationTest.java`（新增 — 5 個 @DisplayName test）
- `docs/grimo/specs/2026-05-08-S154-author-display-identity.md`（同 commit 修 §2.8 V18 SQL + §3 AC-1 簡化 + AC-9 mark dropped + §6 task plan T01 行）

### Verification

- AC-1: ✅ 5/5 V18MigrationTest PASS
- AC-9（原計畫 T01 partial cover）: ❌ DROPPED 2026-05-10（per user 決策）

### Pending verification

無。下一 task T02（User domain entity / repository / UpsertService）依賴 T01 完成。
