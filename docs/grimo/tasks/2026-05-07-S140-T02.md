# S140-T02: Playwright fixtures helper

## Spec
S140 — E2E Critical Path Backfill

## BDD
Given Backend exposes `/internal/test/{reset,seed/skill,seed/download-event}` (T01 done)
When  E2E spec imports `{ test, expect, profiles, seedSkill, seedDownloadEvents } from './_fixtures'`
Then  `test` auto-runs `resetAll(request)` before every test (idempotent state reset)
And   `profiles.empty / single / paged` provide one-call helpers that reset + seed canonical data
And   `seedDownloadEvents()` POSTs to `/internal/test/seed/download-event` with `{ skillId, count, daysAgo }`
And   helper file is derived from `playwright-expert/assets/fixtures-helper-template.ts` (per ADR-007)

## Target Files
- `e2e/tests/_fixtures.ts` (new — derived from template)

## Depends On
- S140-T01

## Status
pending
