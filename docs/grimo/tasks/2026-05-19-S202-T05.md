# S202-T05: Projection seed and semantic fixture data

## 對應規格
S202：Production E2E Fixture Runner

## 這個 task 要做什麼
這個 task 完成後，analytics、quality score、semantic search 這些沒有 production write API 的 read-side data，會由 `e2e/fixtures/projection-seed.ts` 經 DB guard 寫入 disposable `skillshub_e2e` DB。若 semantic E2E case 存在但缺 `SKILLSHUB_E2E_GENAI_API_KEY`，setup project 會在 seed 前 fail，錯誤訊息包含 `semantic E2E requires SKILLSHUB_E2E_GENAI_API_KEY`。

## 使用者情境（BDD）
Given（前提）setup fixtures 已用 production API 建立 `docker-compose-helper`
When（動作）projection seed helper 對該 skill 寫入 download count 5、quality score 92
Then（結果）Analytics UI 可看到 `docker-compose-helper` 與數字 `5`
And（而且）Quality UI 可看到該 skill 的固定分數資料
Given（前提）本輪包含 semantic ranking fixture 且未設定 `SKILLSHUB_E2E_GENAI_API_KEY`
When（動作）執行 `cd e2e && npx playwright test --project "setup fixtures"`
Then（結果）setup fail，錯誤訊息包含 `semantic E2E requires SKILLSHUB_E2E_GENAI_API_KEY`

## 研究來源
- `backend/src/main/java/io/github/samzhu/skillshub/analytics/AnalyticsService.java`
- `backend/src/main/java/io/github/samzhu/skillshub/search/SemanticSearchService.java`
- `backend/src/main/java/io/github/samzhu/skillshub/search/SearchEmbeddingRepository.java`
- `backend/src/main/resources/db/migration/*`
- `e2e/tests/S140-critical-path-analytics.spec.ts`
- `e2e/tests/S140-critical-path-semantic-search.spec.ts`
- `e2e/tests/S193-semantic-search-score.spec.ts`

## 先做 POC
- POC：required — direct SQL 只能碰 projection/read-side data，且 semantic embedding 必須跟 query provider 同一把 key。
- Fixture：
  - `positive-fixture`: guarded DB + existing skill id + downloadCount 5 → analytics API/UI 顯示 5
  - `negative-fixture`: DB guard fail 或 semantic key missing → no SQL executed，setup fail with readable message
- POC 跑完必須印出 `S202 T05 POC PASS`。

## 正式程式怎麼做
- 新增 `e2e/fixtures/projection-seed.ts`
  - `seedProjectionData(db, input)`
  - 只允許寫：
    - `download_events`
    - `skills.download_count`
    - `skill_scores`
    - `skills.embedding`
    - `skills.embedding_content`
    - `skills.embedding_model`
    - `skills.embedding_updated_at`
  - 明確禁止 direct insert aggregate tables：
    - `skills`
    - `skill_versions`
    - `skill_grants`
    - `domain_events`
    - `event_publication`
- 若 semantic fixture enabled：
  - runner 讀 `SKILLSHUB_E2E_GENAI_API_KEY`
  - 缺 key 就 throw `semantic E2E requires SKILLSHUB_E2E_GENAI_API_KEY`
  - 有 key 才對 fixture text 產生 doc embeddings 並寫入 `skills.embedding*`
- 更新 semantic browser specs：
  - 不再依賴 `E2EEmbeddingConfig`
  - 不 assert exact rank quality beyond spec 要求；只 assert semantic route、non-empty、score field、stable UI contract

## 單元測試 / 整合測試
- `e2e/fixtures/projection-seed.spec.ts`
  - `@S202 @AC-S202-6: refuses projection seed when DB guard fails`
  - `@S202 @AC-S202-4: writes download and score projection rows for manifest skill`
  - `@S202 @AC-S202-4: semantic fixture requires SKILLSHUB_E2E_GENAI_API_KEY`

## 會改哪些檔案
- `e2e/fixtures/projection-seed.ts`
- `e2e/fixtures/setup.fixtures.ts`
- `e2e/tests/S140-critical-path-analytics.spec.ts`
- `e2e/tests/S140-critical-path-semantic-search.spec.ts`
- `e2e/tests/S193-semantic-search-score.spec.ts`

## 驗證方式
執行：`cd e2e && npx playwright test --project "setup fixtures" --grep @S202`

## 前置條件
- S202-T04 PASS

## 狀態
PASS

## Result

日期：2026-05-19

### RED

- `cd e2e && npx playwright test --project "fixture unit" --grep "@AC-S202-4|@AC-S202-6"`：FAIL — `Error: Cannot find module .../e2e/fixtures/projection-seed`，代表 projection seed helper 尚未存在，T05 的 DB guard / download / score / semantic key gate 都沒有可驗證入口。
- `cd e2e && npx playwright test --project "setup fixtures" --grep @S202`：FAIL — 第一次實作後 `download_events.event_id` 用 `s202-fixture-<uuid>-N`，PostgreSQL 回 `value too long for type character varying(36)`；修正為 deterministic UUID event_id 後再跑綠。

### GREEN

- `cd e2e && npx playwright test --project "fixture unit" --grep "@AC-S202-4|@AC-S202-6"`：PASS — 7 passed；涵蓋 DB guard fail 不執行 SQL、download/score SQL 只寫 projection/read-side 目標、semantic key 缺失時丟 `semantic E2E requires SKILLSHUB_E2E_GENAI_API_KEY`。
- `cd e2e && npx playwright test --project "setup fixtures" --grep @S202`：PASS — 2 passed；`e2e/results/fixtures.json` 寫入 `expectedDownloadCount: 5` 與 `expectedQualityScore: 92`，console 印出 `S202 T05 POC PASS`。
- `cd e2e && npx playwright test --project "chromium" --grep "AC-6: Analytics dashboard|AC-2: Skill"`：PASS — 4 passed；`/analytics` 顯示 `docker-compose-helper` 與下載數 `5`，skill detail 頁顯示品質 `92%` 與下載次數 `5`。
- `rg -n '/internal/test' e2e/tests e2e/fixtures`：PASS — 只剩 `e2e/fixtures/setup.fixtures.ts` 的 route absence check。
- `git diff --check`：PASS — 無 whitespace error。
- `docker compose -f e2e/compose.e2e.yaml ps --all`：PASS — 無殘留 container。

### Files Changed

- `e2e/fixtures/projection-seed.ts`：新增 `seedProjectionData()` / `seedProjectionDataForDefaultDb()`；guard 通過後可寫 `download_events`、`skills.download_count`、`skill_scores`、`skills.embedding*`，並用 Gemini `embedContent` 產生 semantic fixture embedding。
- `e2e/fixtures/projection-seed.spec.ts`：新增 T05 fixture unit tests，驗 DB guard、projection SQL allowlist、semantic API key gate。
- `e2e/fixtures/setup.fixtures.ts`：setup manifest 建完 production API baseline skills 後，對 `docker-compose-helper` 寫入 download count 5、quality score 92；semantic fixtures 只在 `SKILLSHUB_E2E_SEMANTIC_FIXTURES=true` 時啟用。
- `e2e/tests/_fixtures.ts`：`seedDownloadEvents()` 改走 guarded projection seed helper，不再 fail-fast。
- `e2e/tests/S140-critical-path-analytics.spec.ts`：對齊 S202 shared baseline 10 筆 skill，仍驗 `docker-compose-helper` 下載數 5。
- `e2e/tests/S140-critical-path-skill-detail.spec.ts`：detail 頁改驗 production OAuth author label `Dev-042` 與品質 `92%`。
- `e2e/tests/S140-critical-path-semantic-search.spec.ts`：移除 deterministic ranking / reload 順序假設，保留 semantic route、non-empty、match badge contract。
- `e2e/tests/S193-semantic-search-score.spec.ts`：移除等待舊 async stub embedding 的固定 delay。

### Notes

- `download_events.event_id` schema 是 `VARCHAR(36)`，fixture event id 必須是 UUID 長度；`s202-fixture-<uuid>-N` 會被 DB 拒收。
- Gemini embedding REST API 使用 `models/{model}:embedContent` 與 `output_dimensionality` 產生指定維度向量；來源：[Google Gemini embeddings docs](https://ai.google.dev/gemini-api/docs/embeddings)。
