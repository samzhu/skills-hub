# S154-T04: Command side — Skill 加 authorNameSnapshot + SkillCommandController forgery fix

## Spec
S154 — Author Display Identity (Backend)

## BDD
```
AC-3（forgery fix）+ AC-5（snapshot freeze on publish/republish）

Scenario A: Forgery fix — caller 不帶 author param
Given Bob 已登入（CurrentUserProvider.userId() = "u_bob_xx"）
When Bob POST /api/v1/skills 帶 multipart 不含 `author` 欄位
Then 後端從 currentUserProvider.userId() 取 "u_bob_xx" 寫入 skills.author
And acl_entries[0] = "user:u_bob_xx:OWNER"（owner_id 也是 u_bob_xx）

Scenario B: Forgery fix — caller 偽造 author param
Given Bob 已登入
When Bob POST /api/v1/skills 帶 `author=u_alice_xx`（試圖偽造）
Then 後端 ignore 該 param（或 400 拒收 — 實作擇一），仍寫 "u_bob_xx"
And test 顯示 skills.author = "u_bob_xx"，不是 "u_alice_xx"

Scenario C: Snapshot freeze on initial publish
Given Alice publish skill（CurrentUserProvider.name() = "Alice Chen"）
When skill.publishVersion(...) 觸發 initial publish
Then skills.author_name_snapshot = "Alice Chen"

Scenario D: Snapshot refresh on republish
Given Alice 已 publish skill (snapshot="Alice Chen")
When Alice 改名 "Alice Liu" 後 republish 新版本
Then skills.author_name_snapshot 更新為 "Alice Liu"
```

## Target Files
- `backend/src/main/java/io/github/samzhu/skillshub/skill/domain/Skill.java` (MODIFY — 加 `@Column("author_name_snapshot") private String authorNameSnapshot`；create() / recordVersionPublished() 接 snapshot 參數)
- `backend/src/main/java/io/github/samzhu/skillshub/skill/command/CreateSkillCommand.java` (MODIFY — 加 `String authorNameSnapshot` field)
- `backend/src/main/java/io/github/samzhu/skillshub/skill/command/SkillCommandController.java` (MODIFY — **拒收 `@RequestParam("author")`**；改 `currentUserProvider.userId()` + `currentUserProvider.name()`)
- `backend/src/main/java/io/github/samzhu/skillshub/skill/command/SkillCommandService.java` (MODIFY — publish/republish 流經 snapshot fill；ACL grant 端點接 UserResolver — defer 到 S154b)
- `backend/src/test/java/io/github/samzhu/skillshub/skill/command/SkillPublishForgeryTest.java` (NEW — Scenario A + B)
- `backend/src/test/java/io/github/samzhu/skillshub/skill/SkillAuthorJoinIntegrationTest.java` (NEW — Scenario C + D；同檔覆蓋 T05 ACs，先建框架本 task 寫 snapshot-related cases)

## Depends On
- T03

## Status
pending
