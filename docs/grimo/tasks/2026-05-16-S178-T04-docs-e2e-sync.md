# S178-T04: Documentation And E2E Sync

## 對應規格
S178：Browse Search Entry Point Cleanup

## 這個 task 要做什麼
這個 task 完成後，文件與 E2E 都會把語意搜尋入口指向 `/browse`。使用者在 `/docs/semantic-search` 點 CTA 會到 `/browse`，REST API 文件不再列 `/api/v1/search/intent`，Playwright critical path 也會從 `/browse` 驗證 semantic request，並確認沒有 `/api/v1/skills?keyword=`。

## 使用者情境（BDD）
Given（前提）使用者開 `/docs/semantic-search`

When（動作）他看到語意搜尋試用 CTA

Then（結果）CTA label 是 `前往瀏覽頁試試語意搜尋 →`

And（而且）link target 是 `/browse`

Given（前提）Playwright 開 `/browse`

When（動作）在搜尋框輸入 `images and containers in CI`

Then（結果）browser request log 包含 `/api/v1/search/semantic?q=`

And（而且）browser request log 不包含 `/api/v1/skills?keyword=`

## 研究來源
- `docs/grimo/specs/2026-05-15-S178-browse-search-request-routing.md`
- `frontend/src/pages/docs/SemanticSearchPage.tsx`
- `frontend/src/pages/docs/RestApiPage.tsx`
- `e2e/tests/S140-critical-path-semantic-search.spec.ts`
- `docs/grimo/architecture.md`
- `docs/grimo/debugging-playbook.md`
- `docs/grimo/qa-strategy.md` V07 Playwright happy-path gate

## 先做 POC
- POC：not required — 文件與 E2E 入口調整使用既有 React Router link、既有 Playwright workspace，不新增工具或 fixture API。

## 正式程式怎麼做
- Class / file 名稱：`frontend/src/pages/docs/SemanticSearchPage.tsx`
- 入口：語意搜尋 docs CTA
- 必要行為：
  - CTA label 改成 `前往瀏覽頁試試語意搜尋 →`。
  - `to` 改成 `/browse`。

- Class / file 名稱：`frontend/src/pages/docs/RestApiPage.tsx`
- 入口：REST API docs endpoint table
- 必要行為：
  - 保留 `GET /api/v1/search/semantic`。
  - 移除 `POST /api/v1/search/intent` row。

- Class / file 名稱：`docs/grimo/architecture.md`
- 入口：API endpoint / AI wiring docs
- 必要行為：
  - 把 semantic search endpoint 記成 current runtime `GET /api/v1/search/semantic`。
  - 移除 `searchIntentChatClient` 敘述。

- Class / file 名稱：`docs/grimo/debugging-playbook.md`
- 入口：SearchIntentService native note
- 必要行為：
  - 將 `SearchIntentService.LlmIntentOutput` 相關 active note 移除或標示 obsolete by S178。

- Class / file 名稱：`e2e/tests/S140-critical-path-semantic-search.spec.ts`
- 入口：semantic search happy-path E2E
- 必要行為：
  - 開 `/browse`，不是 `/search?q=...`。
  - 用 search input 輸入 `images and containers in CI`。
  - 收集 request log。
  - 斷言有 `/api/v1/search/semantic?q=`。
  - 斷言沒有 `/api/v1/skills?keyword=`。

## 單元測試 / 整合測試
- `SemanticSearchPage.test.tsx`（若不存在可新增或併入既有 docs page test）
  - `@DisplayName("AC-S178-10: semantic search docs CTA points to browse")`
- `S140-critical-path-semantic-search.spec.ts`
  - `@S178 @happy-path`：browser request log confirms semantic-only browse search contract。

## 會改哪些檔案
- `frontend/src/pages/docs/SemanticSearchPage.tsx`
- `frontend/src/pages/docs/RestApiPage.tsx`
- `frontend/src/pages/docs/SemanticSearchPage.test.tsx`（如需新增）
- `e2e/tests/S140-critical-path-semantic-search.spec.ts`
- `docs/grimo/architecture.md`
- `docs/grimo/debugging-playbook.md`
- `docs/grimo/specs/2026-05-15-S178-browse-search-request-routing.md`

## 驗證方式
執行：

```bash
cd frontend && npm test -- SemanticSearchPage && npm run verify
cd e2e && npx playwright test --grep @S178
git diff --check
```

若 `e2e/node_modules` 不存在，先記錄為 pending verification，並至少跑 `cd frontend && npm test` 確認 docs 端不破。

## 前置條件
- S178-T03 PASS

## 狀態
pending（待做）
