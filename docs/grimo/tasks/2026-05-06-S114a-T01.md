# S114a-T01: ACL 格式遷移 + DB Schema (V16/V17)

## Spec
S114a — RBAC ACL with Materialized Projection (Owner + Viewer roles)

## BDD
Given: 系統有既有 skills，acl_entries 含 `"*:read"` 公開讀取 pseudo-principal
When: 執行 V16/V17 Flyway migration，並更新 production code 改用 3-segment `"public:*:read"` 格式
Then:
- V16 建立 `skill_grants` 表 + `owner_id` NOT NULL column + `is_public` generated column
- V17 將所有既有 `"*:read"` 轉換為 `"public:*:read"`，並從 acl_entries 反推 skill_grants rows（OWNER / VIEWER 分類）
- `Skill.create()` 改輸出 `"public:*:read"` 取代 `"*:read"`
- `AclPrincipalExpander.expand()` 改輸出 `"public:*:read"` 取代 `"*:read"`
- `DelegatingPermissionEvaluator` 改用 `"public:*:read"` (ANONYMOUS_READ_PRINCIPALS + expandPrincipals)
- `SkillAclQueryService.listEntries()` 更新 special-case 識別邏輯
- 所有受影響 tests 改用新格式（不破壞原有 AC 語意）
- `MigrationBackfillTest` (AC-8) PASS

## Target Files
### New
- `backend/src/main/resources/db/migration/V16__rbac_acl_projection.sql`
- `backend/src/main/resources/db/migration/V17__backfill_skill_grants.sql`
- `backend/src/test/java/io/github/samzhu/skillshub/skill/security/MigrationBackfillTest.java`

### Modified (production)
- `backend/src/main/java/io/github/samzhu/skillshub/skill/domain/Skill.java` (create: `"*:read"` → `"public:*:read"`)
- `backend/src/main/java/io/github/samzhu/skillshub/skill/domain/Visibility.java` (更新 Javadoc 引用)
- `backend/src/main/java/io/github/samzhu/skillshub/shared/security/AclPrincipalExpander.java` (`"*:read"` → `"public:*:read"`)
- `backend/src/main/java/io/github/samzhu/skillshub/shared/security/DelegatingPermissionEvaluator.java` (兩處 `"*:read"` → `"public:*:read"`)
- `backend/src/main/java/io/github/samzhu/skillshub/skill/query/SkillAclQueryService.java` (special-case 更新)

### Modified (tests — string replacement only)
- `backend/src/test/java/io/github/samzhu/skillshub/shared/security/AclPrincipalExpanderTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/shared/security/DelegatingPermissionEvaluatorTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/skill/domain/SkillAggregateTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/skill/query/SkillSearchTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/skill/query/SkillAclQueryServiceTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/search/SearchProjectionTest.java`
- `backend/src/test/java/io/github/samzhu/skillshub/community/SkillSubscriptionServiceTest.java`

## Key SQL Notes
### V16__rbac_acl_projection.sql
```sql
-- owner_id
ALTER TABLE skills ADD COLUMN owner_id VARCHAR(255);
UPDATE skills SET owner_id = author WHERE owner_id IS NULL;
ALTER TABLE skills ALTER COLUMN owner_id SET NOT NULL;
CREATE INDEX idx_skills_owner ON skills (owner_id);

-- is_public generated column (checks 3-segment format)
ALTER TABLE skills ADD COLUMN is_public BOOLEAN
  GENERATED ALWAYS AS (acl_entries @> '["public:*:read"]'::jsonb) STORED;
CREATE INDEX idx_skills_is_public ON skills (is_public) WHERE is_public = TRUE;

-- skill_grants table
CREATE TABLE skill_grants (
    id              VARCHAR(36) PRIMARY KEY,
    skill_id        VARCHAR(36) NOT NULL REFERENCES skills(id) ON DELETE CASCADE,
    principal_type  VARCHAR(20) NOT NULL CHECK (principal_type IN ('user','group','company','public')),
    principal_id    VARCHAR(255) NOT NULL,
    role            VARCHAR(20) NOT NULL CHECK (role IN ('OWNER','VIEWER')),
    granted_by      VARCHAR(255) NOT NULL,
    granted_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (skill_id, principal_type, principal_id)
);
CREATE INDEX idx_grants_skill ON skill_grants (skill_id);
CREATE INDEX idx_grants_principal ON skill_grants (principal_type, principal_id);
```

### V17__backfill_skill_grants.sql
```sql
-- 1. Convert "*:read" → "public:*:read" in acl_entries
UPDATE skills
SET acl_entries = (
    SELECT COALESCE(jsonb_agg(
        CASE WHEN entry IN ('*:read', 'public:read') THEN '"public:*:read"'::jsonb
             ELSE to_jsonb(entry) END
    ), '[]'::jsonb)
    FROM jsonb_array_elements_text(acl_entries) entry
)
WHERE acl_entries IS NOT NULL;

-- 2. Backfill OWNER grants from existing acl_entries
INSERT INTO skill_grants (id, skill_id, principal_type, principal_id, role, granted_by, granted_at)
SELECT gen_random_uuid()::text, s.id, parsed.type, parsed.principal,
    CASE WHEN 'delete' = ANY(parsed.perms) THEN 'OWNER' ELSE 'VIEWER' END,
    COALESCE(s.author, 'system'), s.created_at
FROM skills s, LATERAL (
    SELECT split_part(entry, ':', 1) AS type,
           split_part(entry, ':', 2) AS principal,
           array_agg(DISTINCT split_part(entry, ':', 3)) AS perms
    FROM jsonb_array_elements_text(s.acl_entries) entry
    WHERE entry NOT IN ('public:*:read')
      AND split_part(entry, ':', 1) IN ('user','group','company')
    GROUP BY split_part(entry, ':', 1), split_part(entry, ':', 2)
) parsed
ON CONFLICT (skill_id, principal_type, principal_id) DO NOTHING;

-- 3. Backfill public VIEWER grants
INSERT INTO skill_grants (id, skill_id, principal_type, principal_id, role, granted_by, granted_at)
SELECT gen_random_uuid()::text, s.id, 'public', '*', 'VIEWER', COALESCE(s.author, 'system'), s.created_at
FROM skills s
WHERE s.acl_entries @> '["public:*:read"]'::jsonb
ON CONFLICT (skill_id, principal_type, principal_id) DO NOTHING;
```

## Depends On
- none

## Status
PASS

## Result
- V16__rbac_acl_projection.sql: owner_id NOT NULL + is_public GENERATED ALWAYS AS (acl_entries @> '["public:*:read"]') + skill_grants table
- V17__backfill_skill_grants.sql: 3-step backfill (format convert + OWNER grants + public VIEWER grants)
- Skill.java: added ownerId field + getter + set in create() and fromRow()
- SearchProjection.java: 3 occurrences of "*:read" → "public:*:read"
- All 15 test files with raw INSERT INTO skills: added owner_id column
- MigrationBackfillTest.java: 4 AC-8 tests (schema, format migration, OWNER backfill, public VIEWER backfill) — PASS
- SkillAggregateTest.java: all "*:read" → "public:*:read" (incl. last occurrence at line 503)
- Verified: ./gradlew test -x processTestAot -x processAot (targeted tests) — all PASS
