# S154-T06: ACL principal 切換驗證 — 既有 RBAC 測試集 must pass

## Spec
S154 — Author Display Identity (Backend)

## BDD
```
AC-4（ACL principal 切換等價性）

Given V18 backfill 跑完（acl_entries 從 "user:<sub>:OWNER" 改成 "user:<user_id>:OWNER"）
And CurrentUserProvider.current().userId() 回 platform user_id（不是 sub；T03 完工）
And SkillCommandController 寫入 ACL 時用 user_id（T04 完工）
And SkillQueryService LEFT JOIN users（T05 完工）

When 跑既有 RBAC 測試集（無需新增 production code）：
  - SkillSecurityIntegrationTest（owner edit / viewer read / public anonymous read）
  - AclPrincipalExpanderTest（principal pattern 對齊）
  - SkillAclQueryServiceTest（ACL list 端點）
  - SkillCommandServiceAclTest（grant / revoke / 重複授權）
  - 所有以「user:<sub>」hardcode 的舊 test 須 update 成「user:<user_id>」
  - permission denied 場景仍 deny；OWNER edit 仍 allow

Then ./gradlew test 全綠（除 T01-T05 新加 test 外，既有 RBAC 測試 0 fail）
And 任何因 hardcode sub 寫死的 test 已更新為 user_id 對應值

Edge case to verify:
  - V18 backfill 沒 cover 的 principal pattern（如 `group:*:read`、`public:*:read`）保持原樣
  - acl_entries 內非 user prefix 的 entry 不被誤改
```

## Target Files
- `backend/src/test/java/...（RBAC 相關 test）`
  - `SkillSecurityIntegrationTest.java`
  - `AclPrincipalExpanderTest.java`
  - `SkillAclQueryServiceTest.java`
  - `SkillCommandServiceAclTest.java`
  - 任何 hardcode `"user:<sub>"` 的 test
- 預期改動：把 hardcode sub 換成 V18 backfill 後對應 user_id；test fixture data 對齊

## Depends On
- T01（backfill）
- T05（query path 完工才能跑端到端 RBAC）

## Status
pending
