# =============================================================================
# 本地開發機敏設定範例（此檔案提交 Git）
# =============================================================================
# 使用方式：
#   1. 複製此檔案：cp config/application-secrets.properties.example config/application-secrets.properties
#   2. 編輯填入實際值
#   3. 啟動應用程式：./gradlew bootRun
#
# 命名規則：dot-notation（與 @ConfigurationProperties 屬性路徑一致）
#   skillshub.xxx.yyy=value
# GCP Cloud Run 等效 env var：SKILLSHUB_XXX_YYY（relaxed binding 自動對應）
#
# 注意：application-secrets.properties（無 .example）已加入 .gitignore，
#       請勿將含有實際機敏值的檔案提交到版本控制系統。
# =============================================================================

# =============================================================================
# Google GenAI Embedding（語意搜尋）
# =============================================================================
# 從 Google AI Studio (https://aistudio.google.com/apikey) 取得 API key。
# 設定後語意搜尋 (GET /api/v1/search/semantic?q=...) 可回傳真實向量相關結果。
# 未設定時使用 NoOpEmbeddingModel（零向量），語意搜尋回傳空結果（其餘功能正常）。
#
# skillshub.genai.api-key=AIzaSy...
